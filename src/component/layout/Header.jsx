import React from "react";

import "./Header.scss";

/**
 * The component representing the header of the page.
 * @returns {ReactNode}
 * @constructor
 */
const Header = () => {
    return (
        <header>

        </header>
    );
};

export default Header;
